import React from 'react'
import { Header } from 'semantic-ui-react'

export const GameIsFullMessage = ():React.ReactElement => {
    return (
        <Header textAlign='center'>Game is FULL, please come back later</Header>
    )
}
